/// <reference types="vite/client" />

// Injected by vite.config.ts via `define`, sourced from package.json's
// version field at build time (see BrandHeader.tsx for where it's shown).
declare const __APP_VERSION__: string
